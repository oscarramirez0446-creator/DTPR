momodule pc (
    input clk,
    input rst,            
    input [31:0] addnew,
    output reg [31:0] address
);


always @(posedge clk or posedge rst) begin
    if (rst) begin
        address <= 32'b0; 
    end else begin
        address <= addnew;
    end
end

endmodule
